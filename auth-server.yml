server:
  contextPath: /uaa
#  ssl:
#    key-store: classpath: myauth.jks
#    key-store:password: myauth
#    key-password: myauth

jwt:
  token:
    signing:
      key: 123

user:
  name: user
  password: user
  role: USER

admin:
  name: admin
  password: admin
  role: ADMIN

security:
  oauth2:
    client:
      client-id: petstore
      client-secret: petstoresecret
      authorized-grant-types: authorization_code,refresh_token,implicit,password,client_credentials
      scope: abcde
      auto-approve-scopes: abcde

